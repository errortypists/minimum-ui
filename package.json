{
  "name": "minimum-ui",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "kit-build": "yarn workspace minimum-ui rollup -c",
    "kit-watch": "yarn workspace minimum-ui rollup -c -w",
    "docs-start": "yarn workspace minimum-ui-docs start",
    "dev": "concurrently \"yarn kit-watch\" \"yarn docs-start\""
  },
  "devDependencies": {
    "concurrently": "^6.2.1"
  },
  "dependencies": {}
}
